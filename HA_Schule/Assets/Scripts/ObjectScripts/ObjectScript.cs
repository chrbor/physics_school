using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObjectScript : MovementScript
{
    // Start is called before the first frame update
    void Start()
    {
        preVel = Vector2.up;
    }
}
